info:
  name: Cluster - Member List
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/v3/cluster/member/list"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"

settings:
  encodeUrl: true

