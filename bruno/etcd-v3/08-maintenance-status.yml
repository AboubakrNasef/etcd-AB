info:
  name: Maintenance - Status
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/v3/maintenance/status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"

settings:
  encodeUrl: true

