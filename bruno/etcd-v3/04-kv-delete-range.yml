info:
  name: KV - Delete Range
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/v3/kv/deleterange"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "key": "{{key}}"
      }

settings:
  encodeUrl: true

