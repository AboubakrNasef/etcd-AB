info:
  name: Lease - Grant
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/v3/lease/grant"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "TTL": 60
      }

settings:
  encodeUrl: true

